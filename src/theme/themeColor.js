import { createTheme } from "@mui/material/styles";

const themeColor = createTheme({
  palette: {
    primary: {
      main: "#6ab645",
    },
    secondary: {
      main: "#fff",
    },
  },
});

export default themeColor;
